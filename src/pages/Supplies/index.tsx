import { Table } from "components/Table/Table";
import { Layout } from "components/layout";

export const SuppliesPage = () => {
  return (
    <Layout title={`Поставки`}>
      <Table />
    </Layout>
  );
};
